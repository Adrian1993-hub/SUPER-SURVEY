# Assets de marca

Coloca aquí los archivos reales referenciados en `../brand.toml`:

- `logo-light.svg` — logo para fondos claros (UI).
- `logo-dark.svg` — logo para fondos oscuros (UI).
- `logo-report.png` — logo para encabezado de reportes/PDF (alta resolución).

Mientras no existan, la UI/reportes usarán un placeholder neutro.
